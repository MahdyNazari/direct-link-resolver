import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mahdynazari.directlinkresolver',
  appName: 'direct-link-resolver',
  webDir: 'dist',
};

export default config;
