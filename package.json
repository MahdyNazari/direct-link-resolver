{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "direct-link-resolver-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/direct-link-resolver",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm test --workspace=packages/direct-link-resolver"
  }
}